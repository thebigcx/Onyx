#include <onyx/scene/Script.h>
#include <onyx/scene/ScriptableObject.h>

namespace Onyx
{
/*
template<typename T, typename... Args>
void Script<T>::update(float dt)
{
    if (!m_initialized)
    {
        instance = instantiateScript();
        instance->m_owner = object;

        instance->start();

        m_initialized = true;
    }

    if (instance)
        instance->update(dt);
}

template<typename T, typename... Args>
void Script<T>::destroy()
{
    destroyScript(this);
}*/

}